<div class="wrapper">
    <h1>{{pageTitle}}</h1>
    <p>{{description}}</p>

    <form method="post">
        {{# fields}}
            <p>
                {{#properties.label}}
                    <label for="{{properties.id}}">{{properties.label}}</label> <br />
                {{/properties.label}}
                {{{ html }}}
            </p>
        {{/fields}}
        <p>
            <input type="submit" value="Save" class="button primary" />
        </p>
    </form>

</div>

<script type="text/javascript">

    jQuery(document).ready(function(){
        jQuery('#menu_slugs').select2();
        jQuery('#menu_slugs_no_flag').select2();
    })

</script>